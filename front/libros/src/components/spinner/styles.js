import { colors } from "../../utils/constants"

export const styles = {
    container : {
        alignItems: "center",
        justifyContent: "center",
        backgroundColor: colors.textSecondaryVariant,
        bottom: 0,
        left: 0,
        right: 0,
        position: "absolute",
        top: 0,
    }
}